import 'package:flutter/material.dart';

class James extends StatelessWidget {
  const James({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('james')),
    );
  }
}

class Juan extends StatelessWidget {
  const Juan({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Juan')),
    );
  }
}

class Nicholas extends StatelessWidget {
  const Nicholas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Nicholas')),

    );
  }
}
class Sean extends StatelessWidget {
  const Sean({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

class Muriel extends StatelessWidget {
  const Muriel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}

class Anthony extends StatelessWidget {
  const Anthony({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}